//TAIL
//Non Mutating. Returns a new array with every element except the first element.
//Uses assertEqual to test expected length output

//Tail function
const tail = array => array.slice(1);

//Exporting to tailTests.js
module.exports = tail;

